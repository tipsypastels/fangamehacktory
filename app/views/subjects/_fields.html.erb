<div class="subject-fields">
  <% subject.fields.each do |field| %>
    <section class="subject-fields__field">
      <% if subject.show_field_titles? %>
        <h1 class="subject-fields__field__name">
          <%= field.display_name %>
        </h1>
      <% end %>

      <% if field.blank? %>
        <div class="subject-fields__field__blank">
          <%= fa :'empty-set', class: 'subject-fields__field__blank__icon' %>
          
          <h4 class="secondary-text subject-fields__field__blank__name">
            This <%= @subject.term %> has no <%= field.name %>.
          </h1>
        </div>
      <% elsif field.widget? %>
        TODO widget
      <% else %>
        <%= field.content %>
      <% end %>
    </section>
  <% end %>
</div>